title: DriftGuard
description: >-
  Security-Aware Refactoring Assistant for Data Pipelines — parse, lineage,
  drift detection, security scan, and safe SQL refactoring. Zero runtime
  dependencies.
# GitHub Pages deploys this with the native Jekyll pipeline — no build deps.
markdown: kramdown
remote_theme: pages-themes/primer@v0.2.0
plugins:
  - jekyll-remote-theme
show_downloads: true